.pio
wokwi_firmware.bin
*.vsix
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
__pycache__/
*.pyc

# TinyML: everything here is installed or produced locally and is reproducible
# from requirements.txt plus the training scripts. The .gitkeep placeholders are
# already tracked, so the empty data/ and models/ trees still survive a clone.
tinyml/.venv/
tinyml/data/
tinyml/models/
# Compiled on demand by prepare_gestures.py from src/ml/GestureFeatures.h.
tinyml/host/gesture_features
